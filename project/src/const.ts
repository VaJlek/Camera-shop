export const REVIEWS_COUNT = 3;

export enum AppRoute {
  Root = '/',
  Catalog = '/catalog',
  Basket = '/basket',
  Product = '/product',
  NotFound = '/404'
}

export enum APIRoute {
  Cameras = '/cameras',
  Reviews = '/reviews',
  Promo = '/promo',
}

